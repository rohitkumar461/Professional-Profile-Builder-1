# Portfolio Environment Variables
# Copy this file to .env and fill in the values

# Gmail credentials for the contact form (Vercel serverless function)
GMAIL_USER=inbox.rohitkumar@gmail.com
GMAIL_APP_PASSWORD=your_gmail_app_password_here

# Database (only needed if deploying the full API server separately)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: Log level (trace, debug, info, warn, error, fatal)
# LOG_LEVEL=info